{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run build && jest",
    "build": "npm run lint && tsc --outDir build src/index.ts",
    "lint": "tslint src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^22.0.4",
    "typescript": "^2.6.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "0.0.33",
    "ts-jest": "^22.4.1",
    "tslint": "^5.9.1"
  }
}
